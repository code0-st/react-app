import React from 'react';
import styles from './../header.module.css';

const Logo = () => {
    return (
        <div className={styles.logo}>
            Logo
        </div>
    );
}

export default Logo;
